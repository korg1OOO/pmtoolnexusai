/**
 * AboutUs Tests
 */
import { describe, it, expect, vi } from 'vitest';

import AboutUs from '@/pages/AboutUs';

describe('AboutUs', () => {
    it('exports AboutUs', () => {
        expect(AboutUs).toBeDefined();
    });
});
