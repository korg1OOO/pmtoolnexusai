/**
 * CollaborationSpacesView Component Tests
 */
import { describe, it, expect, vi } from 'vitest';

import CollaborationSpacesView from '@/components/views/CollaborationSpacesView';

describe('CollaborationSpacesView', () => {
    it('exports CollaborationSpacesView', () => {
        expect(CollaborationSpacesView).toBeDefined();
    });
});
