/**
 * EVMView Component Tests
 */
import { describe, it, expect, vi } from 'vitest';

import EVMView from '@/components/views/EVMView';

describe('EVMView', () => {
    it('exports EVMView', () => {
        expect(EVMView).toBeDefined();
    });
});
