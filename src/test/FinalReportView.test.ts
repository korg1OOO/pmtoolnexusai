/**
 * FinalReportView Component Tests
 */
import { describe, it, expect, vi } from 'vitest';

import FinalReportView from '@/components/views/FinalReportView';

describe('FinalReportView', () => {
    it('exports FinalReportView', () => {
        expect(FinalReportView).toBeDefined();
    });
});
