/**
 * NotesView Component Tests
 */
import { describe, it, expect, vi } from 'vitest';

import NotesView from '@/components/views/NotesView';

describe('NotesView', () => {
    it('exports NotesView', () => {
        expect(NotesView).toBeDefined();
    });
});
