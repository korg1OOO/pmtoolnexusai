/**
 * PrivacyPolicy Tests
 */
import { describe, it, expect, vi } from 'vitest';

import PrivacyPolicy from '@/pages/PrivacyPolicy';

describe('PrivacyPolicy', () => {
    it('exports PrivacyPolicy', () => {
        expect(PrivacyPolicy).toBeDefined();
    });
});
