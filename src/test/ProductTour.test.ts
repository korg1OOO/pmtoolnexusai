/**
 * ProductTour Tests
 */
import { describe, it, expect, vi } from 'vitest';

import ProductTour from '@/pages/ProductTour';

describe('ProductTour', () => {
    it('exports ProductTour', () => {
        expect(ProductTour).toBeDefined();
    });
});
