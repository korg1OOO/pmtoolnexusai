/**
 * ScheduleDelayPredictor Component Tests
 */
import { describe, it, expect, vi } from 'vitest';

import ScheduleDelayPredictor from '@/components/views/MLAnalytics/ScheduleDelayPredictor';

describe('ScheduleDelayPredictor', () => {
    it('exports ScheduleDelayPredictor', () => {
        expect(ScheduleDelayPredictor).toBeDefined();
    });
});
