/**
 * aspect-ratio Component Tests
 */
import { describe, it, expect, vi } from 'vitest';

import { AspectRatio } from '@/components/ui/aspect-ratio';

describe('aspect-ratio', () => {
    it('exports AspectRatio', () => {
        expect(AspectRatio).toBeDefined();
    });
});
