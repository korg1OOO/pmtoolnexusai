-- Traceability seed data removed
-- Sample data can be added manually via UI after setup
-- This migration is intentionally minimal to avoid schema issues

-- Ensure traceability_matrix table exists (should be created by earlier migrations)
-- If adding seed data later, use proper UUIDs via gen_random_uuid()
